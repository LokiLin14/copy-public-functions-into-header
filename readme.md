# copy-public-functions-into-header

This is a small c program to copy function declarations from the .c files into .h files. It reads a .c file and a .h file and overwrites a section of the .h file with the function declarations. It does this by going looping through every line in the .c file and seeing if it starts with "PUBLIC". If so, it copies it into the section in the .h file. 

For example, running `copy-declarations source.c source.h` results in the following header file: 
```c
struct a_struct_definition {
    int length;
};

// HEADER DECLARATIONS AUTOGENERATED FROM source.c
int this_is_a_function(void);
// END OF HEADER DECLARATIONS AUTOGENERATED FROM source.c
```

## Usage 
```bash 
> copy-declarations
USAGE:
    ./copy-declarations <source> <header> [-h] [-d] 

ARGUMENTS:
    <source>    Source .c file path
    <header>    Header .h file path

OPTIONS:
    -h          Show help
    -d          Dry run, print to stdout instead of overwriting header file
```